- name: Install AUR packages
  become: false
  shell:
    cmd: "paru -S {{ pacakges | join(' ') }} --nouseask --noconfirm --needed --removemake --noupgrademenu --noinstalldebug --cleanafter --nokeepsrc"
